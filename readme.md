1. Update Configuration:
   Make sure you have updated appsettings.json with the correct database connection string and any other configuration changes.

2. Create Database:
   In the project root, you will find a file named Database.sql.
   run the queries in that file to create the database and all necessary tables.

3. Restore & Build Project:
   dotnet restore
   dotnet build
